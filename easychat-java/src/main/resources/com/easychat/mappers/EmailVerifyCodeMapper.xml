<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easychat.mappers.EmailVerifyCodeMapper">

    <resultMap id="base_result_map" type="com.easychat.entity.po.EmailVerifyCode">
        <id column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="code" property="code"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="base_column_list">
        id,email,code,type,status,expire_time,create_time
    </sql>

    <sql id="base_condition_field">
        <if test="query.id!=null">and id = #{query.id}</if>
        <if test="query.email!=null and query.email!=''">and email = #{query.email}</if>
        <if test="query.code!=null and query.code!=''">and code = #{query.code}</if>
        <if test="query.type!=null">and type = #{query.type}</if>
        <if test="query.status!=null">and status = #{query.status}</if>
        <if test="query.currentTime!=null">and expire_time &gt; #{query.currentTime}</if>
    </sql>

    <sql id="base_condition">
        <where>
            <include refid="base_condition_field"/>
        </where>
    </sql>

    <select id="selectList" resultMap="base_result_map">
        select
        <include refid="base_column_list"/>
        from email_verify_code
        <include refid="base_condition"/>
        <if test="query.orderBy!=null">
            order by ${query.orderBy}
        </if>
        <if test="query.simplePage!=null">
            limit #{query.simplePage.start},#{query.simplePage.end}
        </if>
    </select>

    <select id="selectCount" resultType="java.lang.Integer">
        select count(1) from email_verify_code
        <include refid="base_condition"/>
    </select>

    <insert id="insert" parameterType="com.easychat.entity.po.EmailVerifyCode" useGeneratedKeys="true" keyProperty="bean.id">
        insert into email_verify_code
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.email!=null">email,</if>
            <if test="bean.code!=null">code,</if>
            <if test="bean.type!=null">type,</if>
            <if test="bean.status!=null">status,</if>
            <if test="bean.expireTime!=null">expire_time,</if>
            <if test="bean.createTime!=null">create_time,</if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="bean.email!=null">#{bean.email},</if>
            <if test="bean.code!=null">#{bean.code},</if>
            <if test="bean.type!=null">#{bean.type},</if>
            <if test="bean.status!=null">#{bean.status},</if>
            <if test="bean.expireTime!=null">#{bean.expireTime},</if>
            <if test="bean.createTime!=null">#{bean.createTime},</if>
        </trim>
    </insert>

    <insert id="insertBatch" parameterType="com.easychat.entity.po.EmailVerifyCode" useGeneratedKeys="true" keyProperty="id">
        insert into email_verify_code(email,code,type,status,expire_time,create_time) values
        <foreach collection="list" item="item" separator=",">
            (#{item.email},#{item.code},#{item.type},#{item.status},#{item.expireTime},#{item.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" parameterType="com.easychat.entity.po.EmailVerifyCode">
        insert into email_verify_code(id,email,code,type,status,expire_time,create_time) values
        <foreach collection="list" item="item" separator=",">
            (#{item.id},#{item.email},#{item.code},#{item.type},#{item.status},#{item.expireTime},#{item.createTime})
        </foreach>
        on duplicate key update
        status = values(status),
        expire_time = values(expire_time)
    </insert>

    <update id="updateByParam" parameterType="com.easychat.entity.query.EmailVerifyCodeQuery">
        update email_verify_code
        <set>
            <if test="bean.email!=null">email = #{bean.email},</if>
            <if test="bean.code!=null">code = #{bean.code},</if>
            <if test="bean.type!=null">type = #{bean.type},</if>
            <if test="bean.status!=null">status = #{bean.status},</if>
            <if test="bean.expireTime!=null">expire_time = #{bean.expireTime},</if>
            <if test="bean.createTime!=null">create_time = #{bean.createTime},</if>
        </set>
        <include refid="base_condition"/>
    </update>

    <delete id="deleteByParam">
        delete from email_verify_code
        <include refid="base_condition"/>
    </delete>

    <update id="updateById" parameterType="com.easychat.entity.po.EmailVerifyCode">
        update email_verify_code
        <set>
            <if test="bean.status!=null">status = #{bean.status},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from email_verify_code where id = #{id}
    </delete>

    <select id="selectById" resultMap="base_result_map">
        select
        <include refid="base_column_list"/>
        from email_verify_code where id = #{id}
    </select>
</mapper>
