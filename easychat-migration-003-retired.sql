-- ============================================================
-- migration-003：已退役（编号刻意保留，不再复用）
-- ============================================================
-- 原内容：message-read-status（消息已读状态表，配合「已读回执」特性）
--
-- 去向：已读回执特性下线，本迁移随之下线并删除。
--       删除提交：400a054  feat(config): 新增IM能力迁移脚本并下线已读回执残留脚本（2026-09-26）
--
-- 为什么保留编号空缺而不递补：
--   1) 部分环境可能已执行过原 003，复用编号会与既有执行记录 / 备份脚本撞号；
--   2) 迁移序列 001 → 002 → 004… 的断档由此文件显式说明，避免后人误判基线缺迁移。
--
-- 功能层面不缺失：基线 easychat.sql 已包含所有在用的新表结构。
--
-- 本文件仅含注释，无任何可执行语句；即使被误执行也不会产生副作用。
-- ============================================================
