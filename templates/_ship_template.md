# Ship — <版本号或变更名称> 发布检查清单

- 计划发布时间: <YYYY-MM-DD>
- 改动范围: <本次发布包含哪些 Change>

## 代码

- [ ] 所有相关 Change 的 tasks.md 已全部勾选并归档
- [ ] 分支已合入 main 且编译 / 打包 0 error
- [ ] /api/ 前缀与契约语义未变更（或已走 L4 人工确认 + 跨端同步）
- [ ] easychat.sql 已同步所有结构变更

## 数据库

- [ ] 新增表 / 字段已在测试环境验证
- [ ] 存量数据影响已评估（如有字段类型变更）
- [ ] 回滚 SQL 已准备（DROP COLUMN / ALTER 的逆操作）

## 环境

- [ ] application.properties 基线无未评审改动
- [ ] 端口（5050/5051）、Redis 配置、密钥注入方式与生产一致
- [ ] WS 心跳间隔、重连策略符合线上预期

## 部署

- [ ] Docker / 部署脚本已更新（若有）
- [ ] 前端 Electron 打包产物已测试安装流程
- [ ] 冒烟脚本覆盖核心链路（登录 → 发消息 → 收消息 → 刷新会话列表）

## 回滚

- [ ] 回滚步骤：<如何退回上一版本>
- [ ] 数据回滚：<如果数据库结构变了，如何回滚数据>
- [ ] 回滚验证：<回滚后如何验证系统正常>
