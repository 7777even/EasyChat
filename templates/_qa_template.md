# QA — <变更名称（一句话）>

- 日期: <YYYY-MM-DD>
- 效率等级: <L3 / L4>
- 范围: <受影响的包 / 类 / 表 / 端点，列举关键路径与受影响目标。>

## 验收口径

- <逐条列出验收标准，引用 DoD / 契约 / 约束条款。>
- <例：新增端点返回 Result<T> 包络 code=0 才取 data；非法参数返回 400；无权限返回 403。>

## 实际执行命令与结果

- <实际跑了什么命令，通过多少、失败多少。禁止用无关全量命令冒充目标验证。>
- <例：`mvn compile` 0 error；`mvn package -DskipTests` BUILD SUCCESS；接口冒烟 N/N 通过。>

## 未运行项

- <明确哪些没跑、为什么（DB 集成测试未就绪 / 非本变更链路 / 环境限制）。禁止把未执行写成通过。>
- <例：带 DB 的集成测试未引入执行，以 compile + 单元测试冒烟覆盖。>

## 证据附件

- <接口类改动附 curl 请求/响应片段或冒烟终端输出快照；UI 改动附页面截图。随本报告置于同目录（引用文件名，不内嵌二进制），须能佐证"验收口径"逐条达成。>

## 结论

- <一句话结论：达成 / 未达成，遗留项与建议（如另立变更修复与本改动无关的既有缺陷）。>
