# Design — <变更名称>

- 关联 Proposal: <YYYY-MM-DD-<name>/proposal.md>
- 创建日期: <YYYY-MM-DD>

## 1. 架构设计

<改动涉及的总体架构：模块间调用关系、分层职责分配。>

```
<可选：架构图 / 时序图，用 mermaid 或文字描述>
```

### 后端改动

| 模块 | 改动 | 职责边界 |
|------|------|----------|
| Controller | <增删改什么> | 路由 + @Valid + 调 Service |
| Service | <业务规则> | 事务边界 + 权限校验 |
| Mapper / SQL | <数据访问> | 只写 SQL，业务判断在 Service |
| Entity | <持久化对象> | 1:1 对应表结构 |

### 前端改动

| 模块 | 改动 | 职责边界 |
|------|------|----------|
| 主进程 | <ipcMain / WS / db> | 不直接操作渲染进程 DOM |
| preload | <contextBridge 暴露> | 只转发，不写业务逻辑 |
| 渲染进程 | <Vue 组件 / Pinia / request> | 不直接访问 Node.js API |

## 2. 接口设计（如有对外接口变更）

| 端点 | Method | 入参 | 出参 | 权限 |
|------|--------|------|------|------|
| `/api/xxx` | POST | `{...}` | `Result<T>` | 登录用户 |

### 错误码

<新增或复用的错误码，需落在根 AGENTS.md §3.1 对应分段内。>

## 3. 数据模型（如有结构变更）

| 表 | 变更 | 字段 | 类型 | 说明 |
|----|------|------|------|------|
| `xxx` | 新增字段 | `xxx` | VARCHAR(64) | 描述 |

> 变更需同步 `easychat.sql`；字段类型 / 约束变更属 L4。

## 4. 安全设计

- 鉴权: <接口是否需要登录 / 权限校验？>
- 数据权限: <用户是否只能操作自己的资源？>
- 输入校验: <@Valid 规则>
- SQL 注入防护: <是否用 #{}>

## 5. ADR（架构决策记录，可选）

### ADR-001: <决策标题>

- 状态: 已接受 / 已拒绝 / 已废弃
- 上下文: <为什么需要做这个决策？>
- 决策: <选了什么方案？>
- 后果: <选择后的正面和负面结果？>

## 6. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| ... | 高/中/低 | 高/中/低 | ... |

## 7. 依赖与前提

<本变更需要哪些前提条件？其他 Change 或人工准备事项。>
